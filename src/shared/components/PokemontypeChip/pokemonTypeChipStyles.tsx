import { StyleSheet } from "react-native";

const pokemonTypeChipStyles = StyleSheet.create({
  containerChipType: {
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
    borderRadius: 20,
    paddingHorizontal: 15,
    paddingVertical: 4,
  },
});

export default pokemonTypeChipStyles;
