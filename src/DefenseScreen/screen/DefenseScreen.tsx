import React from "react";
import { Text, View } from "react-native";

export const DefenseScreen = () => {
  return (
    <View style={{ flex: 1, justifyContent: "center", alignItems: "center" }}>
      <Text>Pantalla de DefenseScreen</Text>
    </View>
  );
};
